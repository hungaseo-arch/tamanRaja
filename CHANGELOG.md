# 변경 이력

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따른다.
날짜는 작업한 날이며, 그날 저장소에 들어간 커밋을 묶었다.

---

## [2026-09-06] 차월핸디 규칙 확정

모임 규정표대로 차월핸디가 나오지 않던 문제를 바로잡고, 규칙을 앱 안에서
볼 수 있게 했다. DB 트리거·편집 미리보기·화면 도움말·README 네 곳의 규칙을
하나로 맞췄다.

### 변경
- 차월핸디 산출을 모임 규정표에 맞췄다. 비교 대상은 달력상 직전 달이 아니라
  **그 회원이 마지막으로 조에 편성된 라운드**다. 직전 달만 보면 그 달 쉰 사람이
  전부 조 변경으로 몰린다. (`recompute_next_hc`)
- **기준 핸디로 복귀하면 모든 것이 초기화된다.** 당월핸디가 기준핸디와 같은 달
  (매년 1월, 기준핸디를 새로 정한 달, 조 변경으로 복귀한 다음 달)은 직전 조와
  견주지 않고 그 달 결과만으로 ±1 한다.

### 추가
- 월간 기록 화면에 **핸디 규칙** 도움말 창을 넣었다. 용어(기준·당월·차월 핸디,
  조 편성)와 조 조합별 결과표를 담았다.

### 수정
- 2026-10 당월핸디를 새 규칙으로 다시 계산해 7명분을 보정했다.
  2026-07·08·09 기록은 확정된 값이라 손대지 않았고, 새 규칙으로 다시 계산해도
  그 두 달의 저장값과 한 건도 어긋나지 않음을 확인했다.

---

## [2026-09-02] 표 다듬기

- 넓은 화면에서는 열이 다 보이므로 줄 상세 창을 띄우지 않는다.
- 편집 중 행 높이를 연간 랭킹 표와 같게 줄였다.

---

## [2026-08-29] 과거 기록 4년치 · 화면 전면 정비

2022년부터의 기록이 적재되면서 화면이 봐야 할 범위가 4년으로 늘었다.
집계를 뷰로 옮기고, 표·타이포·레이아웃을 화면 전체에 걸쳐 통일했다.

### 추가
- 조회 전용 뷰 `v_meeting_scores`(모임별 개인 성적, Net 계산과 휴면 필터 포함)와
  `v_member_stats`(연도별 + 통산 집계)를 만들었다. 조인과 휴면 필터가 화면마다
  조금씩 달라지던 것을 뷰 한 곳에 못박았다.
- 표에서 이름을 누르면 그 줄의 상세를 모달로 보여준다.
- 참석 확인 창을 계정 창의 탭으로 합치고 메뉴에서 뺐다.
- 나의 기록을 1월~12월 한 해 전체로 고정 표시한다.
- 연간 랭킹에 4등 쇠메달 카드를 넣고, 상단 카드도 표와 함께 스크롤되게 했다.

### 변경
- 과거 기록 조회를 브라우저 조인에서 **뷰 기반 서버측 연도 필터**로 옮겼다.
- 화면을 고정 높이 셸로 바꿔 표의 열 제목이 항상 보인다.
- 헤더의 계정 아이콘을 하나로 줄이고 로그아웃을 계정 창으로 넣었다.
- 본문 폭을 1024px 로 줄이고, 본문 기본 글자를 12px 로 내렸다.
  글자 축소는 표 안으로만 한정했다.
- 툴바 컨트롤 높이를 `size="xs"` 하나로 통일했다.
- Winner/Medalist/Host 배지 정의를 `RankBadge.vue` 한 곳으로 모으고
  Host 색을 보라로 통일했다.
- 쓰이지 않던 다크 모드 코드를 지웠다.

### 수정
- 타입 검사가 실제로 돌지 않고 있었다. 검사를 켜고 드러난 오류를 정리했으며,
  UI 컴포넌트가 네이티브 속성을 받도록 고쳐 `strictTemplates` 를 켰다.
- 좁은 화면에서 표가 카드 밖으로 나가고 편집 입력이 작던 문제를 고쳤다.
- 스크롤할 때 표가 상단 고정 바 위로 지나가던 문제를 고쳤다.
- `main` 의 `max-w-5xl` 때문에 각 화면의 `max-w-7xl` 이 먹지 않던 문제를 고쳤다.
- 연간 랭킹 순위 기준의 분모를 그 해 **이미 치른** 경기 수로 바꿨다.
- 스코어가 아직 없는 달의 차월핸디와 다음 달 당월핸디를 감춘다.
  계산되지 않은 값이 확정된 것처럼 보이면 안 된다.

---

## [2026-08-08] 보안 · 권한 · 랭킹 서버 이전

로그인 검증과 집계가 전부 브라우저에서 이뤄지고 기록 전체가 익명에게 열려
있던 것을 서버로 옮긴 대규모 작업이다. 프런트를 먼저 배포하고 정책을 잠그는
2단계 순서로 적용했다.

### 추가
- 서버측 인증 기반: `app.member_credentials`(bcrypt PIN 해시),
  `app.member_sessions`(sha256 토큰 해시), 그리고 `verify_pin` ·
  `session_member` · `change_pin` · `logout` RPC.
  세션 토큰은 요청 헤더 `x-taman-session` 으로 오간다.
- 연간 랭킹 집계 함수 `yearly_ranking(year)`.
- `app_settings` 테이블. 세 화면에 각각 박혀 있던 핸디 공지 문구를 옮겼다.
- `members.dormant_from` 컬럼. 휴면 명단이 프런트 상수로 박혀 있어 회원 한 명이
  쉬는 것을 배포로만 반영할 수 있었다.
- PWA: `vite-plugin-pwa` 를 넣고 새 버전을 토스트로 알린다. 점수 입력 중에
  말없이 새로고침되지 않도록 `prompt` 방식을 쓴다.
- 공유 카드 이미지와 아이콘 세트.
- 로그인 유지 옵션. 브라우저 저장소 키를 `taman:` 접두사로 통일했다.
- 골프장 셀렉트, 스코어 숫자 입력, 참석 확인 라우트.
- 연간 랭킹·개인 기록 엑셀(CSV) 내려받기.
- 로딩·빈·실패 상태를 공용 `AsyncState` 컴포넌트로 통일하고 저장 실패를 알린다.
- 없는 경로에 404 화면.

### 변경
- PIN 검증을 클라이언트 비교에서 서버 RPC 로 옮기고 `member_pins` 노출을 막았다.
- 쓰기 권한을 서버 정책으로 옮겼다. 규칙은 화면과 같다 —
  관리자 또는 해당 월의 전월 Host(`app.can_manage_month`).
- 읽기도 세션이 있어야 한다. 로그인 화면에 머무는 동안 앱이 모든 테이블을
  받아오고 익명 curl 로도 그대로 읽히던 것을 막았다. `members` 만 로그인
  목록용으로 열어 둔다.
- `golf_courses` 등록 권한을 당월·익월 기획자로 좁혔다.
- 인증 이후에만 데이터를 조회하고 기간 범위로 제한한다.
- 연간 랭킹 순위 기준을 평균 Net 에서 평균 스코어로 바꾸고, 평균 Net·평균
  스코어 열을 누르면 그 지표가 기준이 되게 했다.
- 순위 부여 기준을 그 해 경기의 50% 참석으로 정했다.

### 제거
- 평문 PIN 테이블 `app.member_pins`.
- 다크 모드. 한 번 넣었다가 걷어내고 밝은 화면만 남겼다.

### 수정
- `v_yearly_summary` 가 `security definer` 로 동작해 읽기 RLS 를 우회하던 구멍을
  막았다. 뷰를 `security_invoker` 로 바꿨다.
- 적용 핸디가 없는 달의 Net 을 0 핸디로 계산하던 문제를 고쳤다.
- 미확정 값이 0·빈칸·홀로 남은 구분자로 보이던 문제를 고쳤다.
- 좁은 화면에서 헤더가 무너지고 표의 열을 알아볼 수 없던 문제를 고쳤다.
- 키보드만으로 조작할 수 있게 하고 화면 낭독기용 정보를 보강했다.
- 연간 랭킹에 전체 회원이 나오도록 하고 최소 참석 기준·연도 선택·정렬 방향
  표시를 정리했다.

---

## [2026-07] 월간 기록 편집

- 월간 기록 수정 기능을 넣었다. 미래월 기획과 당월 결과 입력이 같은 표에서 된다.
- 추적되던 `.node_modules_broken` 을 지우고 무시 목록에 넣었다.

---

## [2026-05] Supabase 도입

- 정적 데이터 대신 Supabase(PostgreSQL) 에서 기록을 읽는다.
- React + shadcn-ui 원본을 Vue 3 + TypeScript + TailwindCSS v4 로 옮긴 코드가
  이 시점의 바탕이다.

# 개발 규칙

이 저장소를 고칠 때 지키는 규칙을 모았다. 배경 설명은 [README.md](README.md),
구조는 [ARCHITECTURE.md](ARCHITECTURE.md) 를 본다.

이 모임 앱은 운영 DB 하나만 쓴다. 스테이징도, 되돌릴 백업 절차도 없다.
아래 규칙은 대부분 그 사실에서 나온 것이다.

---

## 1. 작업 진행 방식

- 모든 작업은 **먼저 단계별 Task 를 만들고** 시작한다.
- 각 단계를 마칠 때마다 `[3/8] 단계명 완료` 형식으로 보고한다.
  분자는 방금 끝낸 단계 번호, 분모는 그 작업의 전체 단계 수다.
- 각 항목에 착수하기 전에 관련 파일을 먼저 읽고 현재 구현을 요약해 보고한다.
- 항목을 마치면 그 항목의 완료 조건 기준으로 검증 결과(요청/응답, 화면 확인
  요약)를 함께 보고한다.

## 2. 커밋

- 커밋은 **항목 단위**로 한다. 여러 항목을 한 커밋에 섞지 않는다.
- 제목은 `type(scope): 무엇을 했는지` 형식이고 한국어로 쓴다.
  실제로 쓰는 type 은 `feat` `fix` `refactor` `style` `perf` `chore` `docs` `revert`.
  scope 는 `auth` `rls` `handicap` `monthly` `ranking` `ui` `db` `pwa` 처럼
  바뀐 영역을 적는다.
- 본문에는 "왜" 를 적는다. "무엇" 은 diff 가 이미 말해 준다.

```
fix(auth): PIN 검증을 서버 RPC로 이전하고 member_pins 노출 차단
```

## 3. 데이터베이스

- 스키마·RLS 변경은 `supabase/migrations/` 에 SQL 마이그레이션 파일로 남긴다.
  **Supabase 콘솔에서 수동으로 바꾸지 않는다.**
- 파일 이름은 `YYYYMMDDHHMMSS_설명.sql` 이다. 원격에 적용된 버전 번호와
  로컬 파일 이름이 어긋나면 로컬 파일 이름을 원격에 맞춰 고친다.
  마이그레이션 이력이 저장소와 서버에서 같아야 한다.
- 마이그레이션 파일 머리에 **왜 이 변경이 필요한지**, 그리고 **적용 순서에
  제약이 있는지**를 주석으로 적는다. 지금 저장소의 파일들이 그 예다.
- 운영 DB 의 실제 기록(점수·참석)을 임의로 수정하지 않는다. 규칙이 바뀌어
  과거 값을 다시 계산해야 한다면, 무엇을 왜 바꾸는지 확인받고 그 보정 자체를
  마이그레이션으로 남긴다.
- 새 뷰는 반드시 `security_invoker = on` 으로 만든다. 기본값(definer)으로 두면
  뷰가 읽기 RLS 를 통째로 우회해, 프런트 번들에 든 익명 키만으로 전 회원
  기록이 새어 나간다. 실제로 `v_yearly_summary` 에서 한 번 겪은 구멍이다.
- `members` 는 컬럼 단위로 SELECT 권한을 준다. 컬럼을 새로 추가하면 권한이
  자동으로 따라오지 않는다. 빼먹으면 익명의 members 조회가 통째로 막혀
  로그인 화면의 회원 목록이 빈다.

## 4. 보안

- PIN 값을 로그로 출력하지 않는다. 화면·콘솔·에러 메시지 어디에도 남기지 않는다.
- 서비스 키(`service_role`)를 프런트엔드 코드나 저장소에 포함하지 않는다.
  `.env` 에는 `VITE_SUPABASE_URL` 과 `VITE_SUPABASE_ANON_KEY` 만 둔다.
- **화면의 조건은 권한 경계가 아니다.** 라우터 가드, `v-if="isAdmin"`,
  편집 버튼 노출은 전부 편의일 뿐이다. 같은 규칙을 서버 RLS 가 다시 판정해야
  하고, 최종 판정은 서버 쪽이다.
- 새 테이블을 만들면 RLS 를 켜고 정책을 함께 적는다. 정책 없이 RLS 만 켜면
  전면 차단이고, RLS 를 안 켜면 전면 공개다. 둘 중 무엇을 의도했는지 주석에 적는다.

## 5. 프런트엔드

- 규칙이 두 곳에 있으면 반드시 같아야 한다. 지금 이중으로 구현된 것은 둘이다.
  - 차월핸디: DB 트리거 `recompute_next_hc` 와 `MonthlyTable.vue` 의
    `computeAutoFields()` — 저장 결과와 편집 중 미리보기가 달라지면 안 된다.
  - 연간 랭킹: `yearly_ranking(year)` RPC 와 `getYearlySummary()`
  한쪽을 고치면 다른 쪽도 같은 커밋에서 고친다.
- 타입 검사를 통과해야 한다. `strictTemplates` 가 켜져 있으므로 템플릿의
  prop 타입 오류도 빌드를 막는다.

  ```bash
  npm run type-check
  ```
- 미확정 값은 0 이나 빈칸으로 흘리지 않는다. 핸디 0 은 실제로 있는 값이라
  `coalesce(…, 0)` 같은 처리는 Net 을 조용히 Gross 와 같게 만든다.
- 실패를 삼키지 않는다. 저장 실패는 토스트로 알리고, 인라인 문구는 다시
  저장할 때까지 화면에 남긴다. 토스트는 몇 초 뒤 사라지기 때문이다.
- UI 원자 컴포넌트는 `src/components/ui/` 에 있다. 새 버튼·배지 모양이
  필요하면 화면에서 클래스를 덧대지 말고 그곳의 variant 를 늘린다.
  Winner/Medalist/Host 배지는 `RankBadge.vue` 한 곳에만 정의한다.
- 주석은 한국어로, **왜 이렇게 했는지**를 적는다. 특히 되돌리기 쉬운 결정
  (한 번 겪은 문제를 막는 코드)에는 반드시 남긴다.

## 6. 배포

```bash
git status        # 먼저 확인
npm run deploy    # = npm run build && gh-pages -d dist
```

- `npm run deploy` 는 **작업 트리를 그대로** 빌드해 GitHub Pages 에 올린다.
  커밋되지 않은 변경도 함께 배포되므로, 배포 전에 `git status` 를 확인한다.
- 프런트와 RLS 를 함께 바꾸는 변경은 **프런트를 먼저 배포해 반영을 확인한 뒤**
  정책을 적용한다. 반대로 하면 구버전 사용자에게 빈 화면이 나간다.
  마이그레이션을 1단계(추가 전용)와 2단계(잠금)로 나누는 이유가 이것이다.
- `gh-pages` 브랜치에는 dist 산출물과 `.nojekyll` 만 둔다.
- `vite.config.ts` 의 `BASE` 와 PWA manifest 의 `start_url`·`scope` 는 항상
  같은 값이어야 한다.

## 7. 배포 전 확인

자동화된 테스트가 없다. 아래는 손으로 확인한다.

- [ ] `npm run type-check` 통과
- [ ] `npm run build` 통과
- [ ] 로그인 → 월간 기록 → 연간 랭킹 → 나의 기록 순서로 화면이 뜬다
- [ ] 로그아웃 상태에서 기록이 보이지 않는다
- [ ] 좁은 화면(모바일 폭)에서 표가 카드 밖으로 나가지 않는다
- [ ] 핸디를 건드렸다면, 확정된 달(과거)의 값이 그대로인지 확인한다
